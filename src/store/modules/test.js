const state = {
    test:1
  };
  
  const getters = {
    test: state => state.test,
  };
  
  const actions = {
  };
  
  const mutations = {
  
  };
  
  export default {
    state,
    getters,
    actions,
    mutations
  };